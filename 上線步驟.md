# NIHONGO FIGHTER — 上線到 Railway 步驟

## 🎯 目標
讓遊戲有一個公開網址，例如：
`https://nihongo-fighter.up.railway.app`

---

## ⏱ 總時間：約 10 分鐘

---

## 第一步：安裝 Railway CLI（在 Mac Terminal 執行）

打開你的 **Terminal（終端機）**，貼上以下指令：

```bash
npm install -g @railway/cli
```

---

## 第二步：登入 Railway

```bash
railway login
```

執行後會自動開啟瀏覽器，選擇「**Sign up with Email**」免費註冊一個帳號。

---

## 第三步：進入遊戲資料夾

```bash
cd "/Users/harvey_zzz/Desktop/日文單字網站素材/nihongo-fighter-(日語格鬥家)"
```

---

## 第四步：建立 Railway 專案

```bash
railway init
```

問你「Project name」時輸入：`nihongo-fighter`

---

## 第五步：部署上線 🚀

```bash
railway up
```

Railway 會自動：
1. 上傳你的程式碼
2. 安裝 npm packages
3. 執行 `npm run build`（打包前端）
4. 啟動 `npm run start`（開啟伺服器）

---

## 第六步：取得網址

```bash
railway domain
```

這會顯示你的公開網址，例如：
`https://nihongo-fighter-production.up.railway.app`

---

## ⚙️ 環境變數（可選）

如果你想連接 Notion 資料庫，在 Railway 網站後台加入：

| 變數名稱 | 值 |
|---------|-----|
| `NOTION_API_KEY` | 你的 Notion Token |
| `NODE_ENV` | `production` |

Railway 後台路徑：Project → Variables → New Variable

---

## 💰 費用

- **Free Tier**：每月 500 小時免費（約 20 天）
- **Hobby Plan**：$5/月（無限制）

> 如果只是分享給朋友玩，免費額度完全夠用。

---

## ❓ 常見問題

**Q: 部署失敗怎麼辦？**
A: 執行 `railway logs` 查看錯誤訊息

**Q: 想更新遊戲內容怎麼辦？**
A: 修改程式碼後，再執行一次 `railway up` 就會自動更新

**Q: 網址可以自訂嗎？**
A: 可以購買自己的域名（如 nihongofighter.com）然後在 Railway 設定 Custom Domain
